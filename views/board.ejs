<!DOCTYPE HTML>
<html>
    <head>
        <meta charset="utf-8">
        <title><%=title%></title> 
    </head>

    <link rel='stylesheet' href="/custom.css">

    <body>
     
    <div id="root"> 
        <% include ./menubar.ejs %>

        <div id="side">
        
        </div>

        <div id="con">
            
        <H1 style=""><%=title%></H1> <button style="float:right"onclick="location.href='/board/writeform'">글쓰기</button> <br>
        <Hr style="">
        
            <table style="border:1px solid black; width:800px;">
                <tr>
                    <td style="text-align:center; border:1px solid black">번호</td>
                    <td style="text-align:center; border:1px solid black">제목</td>
                    <td style="text-align:center; border:1px solid black">날짜</td>
                    <td style="text-align:center; border:1px solid black">글쓴이</td>
                </tr>
            <% for ( var i =boards.length - (page*10-10) -1 ; i > boards.length-(page*10) ; i--) { %>
                <% if ( i < 0 ) { break; } %>
                <tr>
                    <td style="text-align:center; width:50px;"> <%=boards[i].idx%> </td>
                    <td style="width:500px;"> <a href="/board/boardview?id=<%=boards[i].idx %>" style="text-decoration:none"><%=boards[i].title %></a> </td>
                    <td style="text-align:center;">  <%=boards[i].date %> </td>
                    <td style="text-align:center;"> <%=boards[i].name%> </td>
                    
                </tr>
                <% } %>
            </table>

            <% for ( var i = 1 ; i < boards.length/10+1 ; i++ ) { %>
            [ <a href="/board?page=<%=i%>"><%=i%></a>  ]
            <% } %>

        </div>
        
 
        <% include ./footer.ejs %>
    </div>
    </body>
</html>